// This component has been deprecated and is no longer in use.
// It was designed to work with the now-removed `/api/capture-email` route.
// The application now uses `SuggestionForm.tsx` which submits directly
// to Formspree.
// This file can be safely removed from the project.

export function EmailCaptureForm() {
  return null;
}
